Sounds in this folder are from **Kenney's Impact Sounds** (https://kenney.nl/assets/impact-sounds),
licensed CC0 (public domain) — no attribution required, credited here anyway.

- `tank-hit.ogg` / `tank-hit-crit.ogg` — TANK hit archetype (was `impactPunch_heavy_000/002.ogg`)
- `dps-hit.ogg` / `dps-hit-crit.ogg` — DAMAGE_DEALER hit archetype (was `impactMetal_light_000.ogg` / `impactMetal_heavy_000.ogg`)

Played via `HTMLAudioElement` in `lib/battle-cards/sfx.ts` (replay) and
`lib/battle-cards/sound.ts` (live PvP) — SUPPORT keeps its synthesized
Web-Audio shimmer since this pack has no fitting magic/chime sample.
